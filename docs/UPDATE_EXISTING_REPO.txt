CONFLICTING ANSWERS — APPLY THE BEFORE YOU TRAVEL UPDATE

This is a complete source replacement based on your uploaded conflicting-answers.zip.
It excludes .git, .env, .venv, local SQLite data, caches and macOS metadata.
Use it to update your existing repository; do not reinitialize Git or replay the old
bootstrap commits. Keep your existing remote and history.

1. PREPARE YOUR EXISTING WORKING COPY

Open Terminal in the root of your existing Git checkout (where app.py lives).
  git status --short
Continue when clean. Commit or safely preserve any unrelated work first.

For a PR directly to main:
  git switch main
  git pull --ff-only origin main
  git switch -c feature/before-you-travel

If your team integrates through develop, start from current develop instead, use
--base develop when creating the PR, and open a develop-to-main release PR afterward.
Do not run both branch-creation alternatives.

2. COPY THE NEW FILES

Extract Conflicting_Answers_Final_Source.zip into a separate directory. It contains
one conflicting-answers folder. Copy its CONTENTS into your existing repository root,
merging directories and replacing same-named source files. Do not replace your entire
repository folder or copy the full delivery ZIP into it.

On macOS/Linux, from your existing repo root, use this after replacing the source path:
  rsync -av --exclude='.git' --exclude='.env' --exclude='.venv' --exclude='catalog.sqlite3' /ABSOLUTE/PATH/TO/EXTRACTED/conflicting-answers/ ./

There is no --delete flag. Existing local credentials, Git history and database remain.
The ZIP cannot be used to move a virtual environment between computers; create a local
virtual environment as needed using the README instructions.

3. REVIEW AND TEST

  git status --short
  git diff --stat
  python3 -m venv .venv
  source .venv/bin/activate
  python -m pip install -r requirements-dev.txt
  python -m pytest -q
  node --test tests/test_planning.cjs

Node is required for the second test suite, not the production app. Optional UI checks:
  npm install --no-save --package-lock=false playwright
  npx playwright install chromium
  node tests/browser.cjs
Stop your existing local server on port 8000 before browser checks.

4. STAGE THE INTEGRATION FILES EXPLICITLY

The following list is the difference from your uploaded source ZIP. If your repository
has changed since that ZIP, inspect those newer changes before committing.

git add -- \
  .github/workflows/ci.yml \
  .gitignore \
  README.md \
  docs/AI_USAGE.md \
  docs/ARCHITECTURE.md \
  docs/DEMO_TRANSCRIPT.md \
  docs/DEPLOY_RENDER_NEON.md \
  docs/GIT_FLOW.txt \
  docs/SOURCES_AND_TRUST.md \
  docs/SUBMISSION_SUMMARY.md \
  docs/UPDATE_EXISTING_REPO.txt \
  docs/VALIDATION.md \
  docs/prs/05-before-you-travel.md \
  public/app.js \
  public/index.html \
  public/planning.js \
  public/styles.css \
  public/sw.js \
  scripts/record_demo.cjs \
  scripts/render_video.py \
  submission/Pitch.pdf \
  tests/browser.cjs \
  tests/test_planning.cjs

  git diff --cached --stat
  git diff --cached
  git commit -m "feat: integrate Before You Travel preparation and selective change review"
  git push -u origin feature/before-you-travel

Create the PR with GitHub CLI, after gh auth login if needed:
  gh pr create --repo El-Moatasem/conflicting-answers --base main --head feature/before-you-travel --title "Integrate Before You Travel preparation and change review" --body-file docs/prs/05-before-you-travel.md

Inspect the actual hosted checks and diff. Merge when satisfied; do not claim reviews
that did not happen. The AI contribution record documents how this code was developed.

5. UPDATE RENDER

Keep your current Render Python service and Neon DATABASE_URL secret. Deploy the
merged main commit (automatically if enabled, otherwise manual deploy). Keep:
  Build: pip install -r requirements.txt
  Start: sh scripts/start.sh
  Health check: /healthz

No database migration is required. This feature derives requirements from the existing
catalog response. Startup preserves that catalog. See docs/DEPLOY_RENDER_NEON.md for
initial setup or reviewed content publication; do not overwrite Neon data just to update UI.

6. VERIFY THE RELEASE

Open the live site online. Confirm scope, prepare items and save the plan. Reopen offline.
In the fictional demo, tick three items and apply the update. Only the receipt step
should reopen. Exit the demo and confirm the real saved plan is unchanged.
Existing v1 snapshots migrate on explicit save. The shell cache has a new version.
If the browser shows an old interface, reload online or use the clear-saved-data action
(the latter intentionally removes saved progress).

7. SUBMISSION FILES

Use the new pitch, demo and written summary from the complete package. The source ZIP
includes README, tests, deployment configuration, pitch PDF, summary and transcript.
The video is a separate MP4; it need not be committed to the app repository.

Status: the update was implemented and tested locally. These instructions have not
pushed changes to your GitHub repository or deployed them to your cloud accounts.
